schemaVersion: 2.2.0
components:
  - name: este-nginx
    image:
      imageName: nginx-tuneada:latest
      autoBuild: true
      dockerfile:
        uri: docker/nginx.Dockerfile
        args:
          - 'MY_ENV=/home/path'
        buildContext: .
        rootRequired: false