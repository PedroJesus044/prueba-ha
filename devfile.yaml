schemaVersion: 2.2.0
components:
  - name: outerloop-build
    image:
      imageName: my-nginx-image:latest
      autoBuild: true
      dockerfile:
        uri: docker/nginx.Dockerfile
        args:
          - 'MY_ENV=/home/path'
        buildContext: .
        rootRequired: false